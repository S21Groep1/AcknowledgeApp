using System;
using Models;
namespace Logic
{
    internal interface IStarr_Logic
    {
        void makeStarr(DateTime edit, string situation, string task, string action, string result, string reflection, Emotion feel);
    }
}